// cypress/integration/insurance_xpath_spec.js

describe('Frontend Test', () => {
  it('test1', () => {
    cy.visit('https://www.ottonova.de/online-beitragsrechner');
   // cy.xpath("//button[contains(text(), 'Alles Akzeptieren')]").click({ force: true });
  });
});